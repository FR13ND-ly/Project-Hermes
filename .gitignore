# Rust / Cargo
backend/target/
backend/Cargo.lock.lock
backend/temp_nginx/
backend/temp_storage/
backend/.env

# Node / Angular
frontend/node_modules/
frontend/.angular/
frontend/dist/
# package-lock.json is committed on purpose: the frontend Dockerfile runs `npm ci`,
# which requires it, and it keeps builds reproducible.

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Temporary files and logs
*.log
first_build_logs.txt
src.zip
update_db.py

# Scratch scripts & AI-tool artifacts (not part of the codebase)
.gemini/
temp_hermes_test/
matching_log.txt
scratch_*.py
backend/.cargo/
backend/src/**/*.py
